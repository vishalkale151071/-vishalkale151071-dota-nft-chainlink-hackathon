import React from 'react';
import '../App.css';
import PlayerCard from './PlayerCard';
const Home = ({account, contract}) => {
    
    
    
    return(
        <div id="cards">
            <PlayerCard />
        </div>
    );
}

export default Home